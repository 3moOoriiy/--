# ⚡ البداية السريعة | Quick Start

## 🎯 في 3 خطوات فقط | Just 3 Steps!

### خطوة 1️⃣: تحميل المشروع
```bash
git clone https://github.com/YOUR_USERNAME/profit-loss-manager.git
cd profit-loss-manager
```

### خطوة 2️⃣: تثبيت المكتبات
**Windows:**
```bash
# اضغط مرتين على setup.bat
# أو:
pip install -r requirements.txt
```

**Mac/Linux:**
```bash
chmod +x setup.sh
./setup.sh
# أو:
pip install -r requirements.txt
```

### خطوة 3️⃣: تشغيل التطبيق
```bash
streamlit run app.py
```

✅ **مبروك!** التطبيق الآن شغال على: http://localhost:8501

---

## 🐛 مشكلة في التثبيت؟

### الخطأ: `ModuleNotFoundError: No module named 'plotly'`

**الحل:**
```bash
pip install plotly --upgrade
```

### الخطأ: `command not found: streamlit`

**الحل:**
```bash
pip install streamlit --upgrade
```

### مشاكل أخرى؟
افتح ملف **TROUBLESHOOTING.md** للحلول الكاملة!

---

## 🌐 نشر على الإنترنت (مجاناً)

### 1. ارفع المشروع على GitHub
```bash
git add .
git commit -m "Initial commit"
git push origin main
```

### 2. اذهب إلى Streamlit Cloud
https://share.streamlit.io/

### 3. اختر المشروع واضغط Deploy!

**خلاص! تطبيقك أصبح على النت 🎉**

---

## 📝 أوامر سريعة | Quick Commands

```bash
# تشغيل التطبيق
streamlit run app.py

# مسح الـ cache
streamlit cache clear

# تحديث المكتبات
pip install -r requirements.txt --upgrade

# فحص المكتبات المثبتة
pip list

# إنشاء بيئة افتراضية جديدة
python -m venv venv

# تفعيل البيئة (Windows)
venv\Scripts\activate

# تفعيل البيئة (Mac/Linux)
source venv/bin/activate
```

---

## 💡 نصائح مهمة | Important Tips

1. **استخدم بيئة افتراضية دائماً**
   - يحافظ على المكتبات منظمة
   - يتجنب تعارض الإصدارات

2. **اختبر محلياً أولاً**
   - تأكد أن كل شيء يعمل
   - ثم انشر على الإنترنت

3. **اقرأ الأخطاء بعناية**
   - معظم الأخطاء واضحة
   - ابحث عنها في Google

4. **اعمل Backup للبيانات**
   - `transactions.json` يحتوي على كل بياناتك
   - انسخه بانتظام

---

## 📚 ملفات إضافية مفيدة

- **README.md** - الدليل الشامل الكامل
- **TROUBLESHOOTING.md** - حل جميع المشاكل المحتملة
- **GIT_SETUP_GUIDE.md** - شرح Git من الصفر

---

## 🎓 تعلم أكثر | Learn More

**Streamlit:**
- الموقع الرسمي: https://streamlit.io
- الدروس: https://docs.streamlit.io/get-started

**Python:**
- Python.org: https://www.python.org
- دروس بالعربي: YouTube "Python Arabic"

**Git & GitHub:**
- Git Docs: https://git-scm.com
- GitHub Guides: https://guides.github.com

---

## ❓ أسئلة شائعة | FAQ

**س: كيف أحفظ البيانات بشكل دائم؟**
ج: البيانات محفوظة في `transactions.json`. للنشر على Cloud، استخدم قاعدة بيانات خارجية.

**س: هل يمكن استخدامه لأكثر من مستخدم؟**
ج: حالياً لا. لكن يمكن إضافة نظام تسجيل دخول.

**س: كيف أغير الألوان والتصميم؟**
ج: عدّل ملف `.streamlit/config.toml`

**س: هل هو مجاني؟**
ج: نعم! مفتوح المصدر ومجاني 100%

---

**🚀 استمتع بإدارة أرباحك بشكل احترافي!**

**Made with ❤️ | صُنع بحب**
